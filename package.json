{
  "name": "selenium",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "mocha --no-timeouts --parallel --reporter mochawesome --require mochawesome/register",
    "build": "mocha --no-timeouts --parallel --reporter mochawesome --require mochawesome/register ./test/*.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.3.6",
    "chromedriver": "^103.0.0",
    "expect": "^28.1.3",
    "manager": "^1.0.9",
    "mocha": "^10.0.0",
    "mochawesome": "^7.1.3",
    "selenium-webdriver": "^4.2.0",
    "webdriver": "^7.20.8"
  }
}
